export function Philosophy() {
  return (
    <section className="band bg-near">
      <div className="shell flex justify-center">
        <blockquote className="max-w-[1020px] text-center font-display text-[length:var(--text-quote)] font-light italic leading-[1.5] text-cream">
          &ldquo;A room is finished when nothing else needs explaining — when
          the hinge, the handle and the light all agree.&rdquo;
        </blockquote>
      </div>
    </section>
  );
}
